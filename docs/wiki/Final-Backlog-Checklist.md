# Final Backlog Checklist (CW2 Traceability Matrix)

This page serves as the **final requirements traceability matrix** for the CW2 assessment, mapping each backlog requirement (ID1–ID25) to its delivery status, concrete implementation evidence in the codebase, and the presence/absence of automated test coverage.

| ID | Requirement Description | Status | Implementation Evidence | Test Coverage |
|---:|---|---|---|---|
| 1 | Support user accounts and user login | **Fully Implemented** | `src/backend/routes/auth.js` (POST `/register`, POST `/login`)<br>`src/backend/auth-service.js` (scrypt hashing; base64url session tokens)<br>`src/backend/database.js` (`createUser`, `findUserByEmail`)<br>`src/backend/roles.js` (canonical role model)<br>`src/backend/auth-middleware.js` (`authenticateRequest`)<br>`database/schema.sql` (users table)<br>`src/frontend/components/AuthManager.jsx` (login/register UI; password confirm; show/hide)<br>`src/frontend/components/Layout.jsx` (logout)<br>`src/frontend/session.js` (localStorage persistence) | **Yes** — `tests/auth-rbac.test.js`, `tests/integration/auth-flow.test.js` |
| 2 | Option to store customer's card details for quicker bookings | **Fully Implemented** | `database/schema.sql` + `database/migrations/003_add_stored_cards.sql` (stored_cards table: last4 + brand + SHA-256 hash; never raw PAN/CVV)<br>`src/backend/routes/cards.js` (POST/GET/DELETE `/api/cards`; rider self-service only)<br>`src/backend/booking-service.js` (`hashCardPan`, `extractLast4`, `detectCardBrand`)<br>`src/backend/routes/bookings.js` (POST `/api/bookings` accepts optional `savedCardId`; resolves via `SIMULATOR_CARD_BY_HASH` map)<br>`src/frontend/components/SavedCards.jsx` (manage saved cards: list/add/delete; dev-only simulator hint)<br>`src/frontend/components/ScooterList.jsx` (booking modal: saved card vs manual entry toggle)<br>`src/frontend/App.jsx` + `src/frontend/components/Layout.jsx` (route `/cards` + "My Cards" nav link) | **Yes** — `tests/integration/cards-rbac.test.js` (18 tests: save/list/delete; cross-user isolation; booking with savedCardId; invalid id; wrong user) |
| 3 | If ID2: good security for user accounts | **Fully Implemented** | Card data security: only `card_last4` + SHA-256 `card_hash` persisted; raw PAN and CVV never stored in DB or exposed in GET responses. Hashing is deterministic SHA-256 of normalised PAN (coursework simulation limitation documented in code comments). `POST /api/cards` validates via `validatePaymentPayload` before hashing. `POST /api/bookings` with `savedCardId` resolves the stored hash to a simulator card number via a compile-time constant map — the full PAN is never stored in the DB and the two known test PANs are resolved in application code only. `DELETE /api/cards/:id` enforces owner-only. Frontend `requestJson` always sends `Authorization` header. | **Yes** — `tests/integration/cards-rbac.test.js` (verifies no `cardHash` in GET response; 403 for cross-user delete/booking; 401 for unauthenticated access) |
| 4 | View hire options and cost: 1hr, 4hrs, 1day, 1week | **Fully Implemented** | `database/schema.sql` (scooter pricing: `one_hour`, `four_hours`, `one_day`, `one_week`)<br>`src/backend/routes/scooters.js` (GET `/api/scooters` returns pricing per scooter)<br>`src/frontend/components/ScooterList.jsx` (hire plan cards; pricing grid)<br>`src/frontend/components/ScooterMap.jsx` (popup shows hourly rate) | **Yes** — `tests/integration/scooters-rbac.test.js` (verifies GET `/api/scooters` includes pricing) |
| 5 | Book an e-scooter; select e-scooter ID and hire period | **Fully Implemented** | `src/backend/routes/bookings.js` (POST `/api/bookings` validation + availability checks)<br>`src/backend/booking-service.js` (`createBookingInTransaction` atomic reserve+insert)<br>`src/frontend/components/ScooterList.jsx` (booking modal: scooter selection; duration; confirmation details) | **Yes** — `tests/integration/bookings-rbac.test.js` (POST `/api/bookings` 201 for rider) |
| 6 | Handle card payment for booking (simulated) | **Fully Implemented** | `src/backend/booking-service.js` (`validatePaymentPayload`; `simulatePayment` deterministic accept/decline)<br>`src/frontend/components/ScooterList.jsx` (payment form: `cardholderName`, `cardNumber`, `expiryDate`, `cvv` + simulator hint)<br>`src/backend/routes/bookings.js` (POST `/api/bookings` validates + simulates payment; returns `paymentStatus`, `paymentReference`) | **Yes** — `tests/integration/bookings-rbac.test.js` (uses `SAMPLE_PAYMENT` with `4242...`) |
| 7 | Send booking confirmation via email | **Fully Implemented** | `src/backend/email-service.js` (SMTP via nodemailer; `buildSmtpConfig()` supports Mailpit relay, QQ Mail defaults, or disabled; env: `SMTP_HOST`/`SMTP_PORT`/`SMTP_USER`/`SMTP_PASS`/`SMTP_FROM`/`SMTP_SECURE`; three email types: registration welcome, booking confirmation, booking completed; fire-and-forget via `sendMailBestEffort()`; errors logged, never thrown)<br>`src/backend/routes/auth.js` (`sendRegistrationEmail` on POST `/api/auth/register`)<br>`src/backend/routes/bookings.js` (`sendBookingConfirmationEmail` after rider booking and staff walk-in with guest email; `sendBookingCompletedEmail` on cancel)<br>`src/backend/server.js` (`dotenv` loads `.env`; startup warning when email disabled)<br>`.env.example` (SMTP variables documented) | **Yes** — `tests/email-service.test.js` (8 tests: disabled, QQ defaults, Mailpit relay, transport send/throw, registration/confirmation/completed content); `tests/integration/bookings-rbac.test.js` (booking 201 with SMTP enabled but unreachable) |
| 8 | Store booking confirmation and display on demand | **Fully Implemented** | `database/schema.sql` (bookings table with `user_id`, `scooter_id`, `duration_code`, `total_price`, `status`, timestamps)<br>`src/backend/routes/bookings.js` (GET `/api/bookings/me` returns bookings ordered by `created_at` DESC)<br>`src/frontend/components/MyBookings.jsx` (booking history UI; empty/error states) | **Yes** — `tests/integration/bookings-rbac.test.js` (POST then cancel verifies persistence) |
| 9 | (Staff) Take bookings for unregistered users (requires ID7) | **Fully Implemented** | `src/backend/routes/bookings.js` (POST `/api/admin/bookings`: staff/admin-gated via `requireStaff`; accepts `{ scooterId, durationCode, payment, guestEmail?, guestName? }`; resolves walkin user or uses internal placeholder; reuses `createBookingInTransaction`; fire-and-forget email via `sendBookingConfirmationEmail` when guest email provided; skipped for `walkin@escooter.internal`)<br>`src/backend/roles.js` (`walkin` added to `ALL_USER_TYPES`; not self-registrable)<br>`src/backend/routes/auth.js` (explicit 401 rejection for walkin login)<br>`database/schema.sql` + `database/migrations/002_add_walkin_user_type.sql` (expanded CHECK constraint; seeded `walkin@escooter.internal` with placeholder credentials)<br>`src/frontend/components/AdminBookings.jsx` (“Book Walk-in” button + modal with scooter/plan/payment/guest fields)<br>`src/frontend/roles.js` (“Walk-in” label in `getRoleLabel`) | **Yes** — `tests/integration/walkin-booking-rbac.test.js` (rider 403; staff 201 with/without guest email; walkin login 401); `tests/auth-rbac.test.js` (`normalizeUserType` preserves walkin) |
| 10 | (From ID5) Update e-scooter status from available to unavailable | **Fully Implemented** | `src/backend/booking-service.js` (`createBookingInTransaction`: `UPDATE scooters SET status='in_use' ... WHERE status='available'`; rollback if 0 rows updated)<br>`src/backend/routes/bookings.js` (cancel sets scooter back to `available`)<br>`database/schema.sql` (scooters status CHECK includes `in_use`)<br>`src/frontend/components/ScooterList.jsx` (“Book now” disabled unless `status === 'available'`) | **Yes** — `tests/integration/bookings-rbac.test.js`, `tests/integration/scooters-rbac.test.js` (“DELETE while in_use returns 409”) |
| 11 | Option to extend current booking | **Fully Implemented** | `src/backend/routes/bookings.js` (PATCH `/api/bookings/:bookingId/extend`: ownership + active status; enforces longer plan via `DURATION_RANK`; recalculates price from `scooter_pricing`)<br>`src/frontend/components/MyBookings.jsx` (extend UI: only longer plans shown; confirm/cancel; success messaging) | **Yes** — `tests/integration/bookings-rbac.test.js` (extend 200 with valid longer plan; 400 when shorter/equal; 403 for another user) |
| 12 | Cancel booking | **Fully Implemented** | `src/backend/routes/bookings.js` (PATCH `/api/bookings/:bookingId/cancel`: ownership + active status; atomic update booking→`completed` and scooter→`available`)<br>`src/frontend/components/MyBookings.jsx` (cancel UI; disabled while running; success/error; refetch) | **Yes** — `tests/integration/bookings-rbac.test.js`, `tests/integration/scooters-rbac.test.js` |
| 13 | Send short feedback for issues/faults | **Fully Implemented** | `src/backend/routes/issues.js` (POST `/api/issues`: authenticated users can submit `scooterId` + `description`; stored with `priority='low'`, `status='open'`)<br>`database/schema.sql` (issues table)<br>`src/backend/database.js` (`createIssue`)<br>`src/frontend/components/ReportIssue.jsx` (customer-facing form: scooter dropdown + description textarea; confirmation summary on success; loading/error/empty states)<br>`src/frontend/App.jsx` (route `/report-issue` wired to ReportIssue component)<br>`src/frontend/components/Layout.jsx` (“Report Issue” link in customer nav via CUSTOMER_NAV_ITEMS) | **Yes** — `tests/integration/issues-rbac.test.js` (POST `/api/issues` 201 for authenticated rider); no dedicated frontend component test |
| 14 | If ID13: Prioritise feedback (escalate high priority; resolve low priority) | **Fully Implemented** | `src/backend/routes/issues.js` (PATCH `/api/issues/:id/priority` low↔high; PATCH `/api/issues/:id/status` open↔resolved; staff/admin gated)<br>`src/backend/database.js` (`updateIssue` supports priority/status)<br>`src/frontend/components/AdminIssues.jsx` (Escalate/De-escalate; Resolve/Reopen; loading + success/error states)<br>`src/backend/auth-middleware.js` (`requireStaff` on PATCH routes) | **Yes** — `tests/integration/issues-rbac.test.js` (RBAC: rider 403; staff/admin 200) |
| 15 | If ID14: View high priority issues | **Fully Implemented** | `src/backend/routes/issues.js` (GET `/api/issues` supports `?priority=high`; validates against `ALLOWED_ISSUE_PRIORITIES`)<br>`src/backend/database.js` (`getIssues` supports priority filter)<br>`src/frontend/components/AdminIssues.jsx` (priority filter dropdown; combined with status filter) | **Yes** — `tests/integration/issues-rbac.test.js` (GET `/api/issues` 200 for staff/admin; verifies array response) |
| 16 | Configure e-scooter details and costs | **Fully Implemented** | `src/backend/routes/scooters.js` (admin CRUD: POST create; PUT update; DELETE soft-retire)<br>`src/backend/scooter-service.js` (`validateScooterPayload`: scooterId format; allowed statuses; lat/lng range; non-negative pricing)<br>`database/schema.sql` (scooters + `scooter_pricing` tables)<br>`src/frontend/components/AdminFleet.jsx` (create/edit forms; client-side validation; retire/reactivate; duplicate ID check) | **Yes** — `tests/scooter-service.test.js`, `tests/integration/scooters-rbac.test.js` |
| 17 | Display scooter list availability (availability/location if available) | **Fully Implemented** | `src/backend/routes/scooters.js` (GET `/api/scooters` excludes retired; returns status + location per scooter)<br>`src/frontend/components/ScooterList.jsx` (status counts; per-scooter status + location; availability summary)<br>`src/frontend/components/ScooterMap.jsx` (colour-coded markers + popup details) | **Yes** — `tests/integration/scooters-rbac.test.js` (public GET `/api/scooters` returns seed rows) |
| 18 | Display the five scooter locations on a visual map | **Fully Implemented** | `src/frontend/components/ScooterMap.jsx` (Leaflet + OpenStreetMap tiles; Leeds center; status-colour markers; popup shows scooterId/status/location/hourly rate; legend; stable marker rendering to avoid flicker)<br>`src/frontend/hooks/useScooters.js` (data source) | **No** — no dedicated map component tests (API endpoint covered by scooters RBAC tests) |
| 19 | View weekly income for rental options (1hr, 4hr, day, week) | **Fully Implemented** | `src/backend/routes/bookings.js` (GET `/api/bookings/income/weekly`: admin-only; aggregates SUM+COUNT grouped by `duration_code` for 7-day window; defaults to current Monday; returns `grandTotal` etc.)<br>`src/frontend/components/Income.jsx` (admin income dashboard; per-plan cards; week navigation; loading/error/empty states) | **Yes** — `tests/integration/bookings-rbac.test.js` (401/403/200; validates `weekStart` + `grandTotal`) |
| 20 | View combined daily income over a week duration | **Fully Implemented** | `src/backend/routes/bookings.js` (GET `/api/bookings/income/daily`: admin-only; groups by date + duration_code; 7 zero-filled days; `weekStart` must be a Monday; per-day totals + breakdown + `grandTotal`)<br>`src/frontend/components/Income.jsx` (By Plan / By Day toggle; daily BarChart with per-plan tooltip; preserves `data-id="ID21"` for weekly chart) | **Yes** — `tests/integration/bookings-rbac.test.js` (401/403/200 with 7-day structure validation; rejects non-Monday weekStart) |
| 21 | If ID19/20: Plot weekly income graphically | **Fully Implemented** | `src/frontend/components/Income.jsx` (recharts `BarChart` + `ResponsiveContainer`; X axis = hire plan; Y axis = currency; custom tooltip; semantic `figure` with `aria-label`; `data-id="ID21"`)<br>`package.json` (includes `recharts`) | **No** — no dedicated chart rendering test (visual component) |
| 22 | Discount for frequent users (8+ hrs/week), students, senior citizens | **Fully Implemented** | `src/backend/routes/bookings.js` (`DISCOUNTED_USER_TYPES` = {student, senior}; frequent threshold = 8 hours; multiplier \(0.8\); booking response includes `discountApplied` + `originalPrice`)<br>`src/backend/booking-service.js` (`calculateWeeklyUserHours`: sums last 7 days’ bookings; maps duration_code→hours; ignores unknown codes) | **Yes (indirect)** — exercised via POST `/api/bookings` flow in `tests/integration/bookings-rbac.test.js` |
| 23 | Support usage by multiple clients simultaneously | **Fully Implemented** | `src/backend/database.js` (`AsyncMutex` serializes SQLite writes via `runExclusive()` to prevent `SQLITE_BUSY` across write paths)<br>`src/backend/server.js` (Express request concurrency via Node event loop)<br>`src/backend/db/connection.js` (single SQLite connection; `PRAGMA foreign_keys=ON`)<br>Test runner uses `--test-concurrency=1` for SQLite safety, not a runtime limitation. | **Yes (architectural)** — integration suite validates correctness under serialized write pattern |
| 24 | Provide a responsive user interface | **Fully Implemented** | `src/frontend/styles.css` (multiple responsive breakpoints: 1100px/900px/768px/600px; layout collapse; stacked panels; fluid sizing; responsive chart container) | **No** — visual/responsive verification only |
| 25 | Address accessibility (colour & font choices, etc.) | **Fully Implemented** | `src/frontend/styles.css` (CSS variables for color scheme; high-contrast palette; legible font stack)<br>`src/frontend/components/` (ARIA attributes across interactive UI; `aria-live` for dynamic content; `aria-invalid` for password mismatch; semantic HTML; dialog labeling) | **No** — accessibility audit would require manual/automated a11y tooling |

